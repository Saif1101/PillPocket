import 'package:flutter/material.dart';

class UpcomingReminderCard extends StatelessWidget {
  //ReminderEntity as argument 
  const UpcomingReminderCard({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}